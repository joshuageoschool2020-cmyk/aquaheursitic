# Heuristic OS V3.1 — Production Dependencies
# Render deployment: pip install -r requirements.txt

# Core API
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# Environment config
python-dotenv==1.0.1

# Testing (CI only — safe to leave in, Render ignores unused packages)
httpx==0.27.0
pytest==8.2.0
pytest-asyncio==0.23.7